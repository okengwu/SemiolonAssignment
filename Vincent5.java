// author - chidi
/* compare integer using if statement, relational operators
and equality operators */
import java.util.Scanner; // program uses class Scanner 

public-0 class Vincent5 {
    
    public void main(String... args) {
        Scanner input = new Scanner(System.in);
        
        System.out.print("Enter the first integer");
        int num1 = input.nextInt(); // read first number from user

        System.out.print("Enter the second integer"); // prompt
        int num2 = input.nextInt(); // read first number from user

        if (num1 == num2)
        System.out.printf("%d == %d%n", num1 , num2);

        if (num1 != num2) {
            System.out.printf("%d != %d%n" , num1, num2);

        if (num1 < num2) {
            System.out.printf("%d < %d%n", num1, num2);
        
        if (num1 > num2) {
            System.out.printf("&d > %d%n", num1, num2);
        if (num1 <= num2) {
            System.out.printf("%d <= %d%n" , num1, num2);

        if (num1 >= num2) {
            System.out.printf("%d >= %d%n", num1, num2);
        }
        }
        }

        }
        }
        
        } // end of it 
              
    } // end class comparism



